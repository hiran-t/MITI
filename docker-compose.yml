version: '3.8'

services:
  # MITI Web Application
  miti-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: miti-web
    ports:
      - "3000:3000"
    environment:
      # Point this at your ROS2 machine running rosbridge_server
      # If using the rosbridge service below, keep ws://rosbridge:9090
      # If using an external robot, change to ws://<robot-ip>:9090
      - NEXT_PUBLIC_ROSBRIDGE_URL=ws://rosbridge:9090
    networks:
      - miti-network
    restart: unless-stopped

  # Rosbridge Server (optional)
  # Uncomment to run rosbridge inside Docker alongside MITI
  # Note: requires a running ROS2 environment on the host
  # rosbridge:
  #   image: ros:humble
  #   container_name: miti-rosbridge
  #   command: >
  #     bash -c "
  #       apt-get update &&
  #       apt-get install -y ros-humble-rosbridge-suite &&
  #       source /opt/ros/humble/setup.bash &&
  #       ros2 launch rosbridge_server rosbridge_websocket_launch.xml
  #     "
  #   ports:
  #     - "9090:9090"
  #   networks:
  #     - miti-network
  #   restart: unless-stopped

networks:
  miti-network:
    driver: bridge

# Usage:
# 1. Build and start all services:
#    docker-compose up -d
#
# 2. View logs:
#    docker-compose logs -f
#
# 3. Stop all services:
#    docker-compose down
#
# 4. Rebuild after code changes:
#    docker-compose up -d --build
